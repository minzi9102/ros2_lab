## 强制执行规则（新增）
以下规则默认启用，除非用户明确给出相反指令：

1. 每次代码修改 / 重构 / 调试后：必须调用 `$git-md-micro-commit` 进行存档，按 Markdown-Tagged Micro-Commit 结构生成并提交；不询问用户。
2. 每次执行任何代码修改前：先阅读当前 Git 仓库至少 20 条提交记录；若历史上下文不足以支撑判断，必须继续阅读更多提交记录后再执行修改。
3. 每次任务只能进行一个功能的开发 / 改动；若发现需求跨越多个功能，需拆分为多个独立任务并逐个完成。